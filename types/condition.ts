// The third value of the array is all the possible types in
// `questionTypes`. There's probably a better way to type this,
// but I don't know it. Left as a TODO
export type Condition = [number, "=", string | boolean];
